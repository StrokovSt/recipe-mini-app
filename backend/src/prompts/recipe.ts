export const recipePrompt = (text: string) => `
Извлеки рецепт из текста. Верни ТОЛЬКО валидный JSON без markdown и пояснений.

Правила:
- ingredients: копируй дословно из текста, каждый ингредиент отдельной строкой
- steps: копируй дословно из текста. Если шаг обрезан или текст неполный — восстанови его логически на основе ингредиентов, но не добавляй шаги которых нет
- time: бери из текста, если нет — выведи из шагов
- servings: только если явно указано в тексте, иначе null
- tags: 3-5 коротких ключевых слов для поиска

Формат ответа:
{
    "title": "название блюда",
    "ingredients": ["ингредиент 1", "ингредиент 2"],
    "steps": ["шаг 1", "шаг 2"],
    "time": "время приготовления или null",
    "servings": число или null,
    "tags": ["тег1", "тег2"]
}

Если рецепта нет — верни { "error": "Рецепт не найден" }

Текст: ${text}
`;