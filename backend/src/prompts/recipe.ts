export const recipePrompt = (text: string) => `
Извлеки данные из текста и верни ТОЛЬКО валидный JSON без markdown и пояснений.

Правила:
- Всегда возвращай объект рецепта, даже если текст не содержит рецепта
- title: название блюда или заголовок поста. Если нет — придумай по контексту
- ingredients: копируй дословно. Если нет — пустой массив []
- steps: копируй дословно. Если шаг обрезан — восстанови по контексту. Если нет — пустой массив []
- time: только если явно указано, иначе null
- servings: только если явно указано, иначе null
- tags: 3-5 ключевых слов по контексту, всегда заполняй

Формат ответа:
{
    "title": "название",
    "ingredients": ["ингредиент 1"],
    "steps": ["шаг 1"],
    "time": "время или null",
    "servings": число или null,
    "tags": ["тег1", "тег2"]
}

Текст: ${text}
`;